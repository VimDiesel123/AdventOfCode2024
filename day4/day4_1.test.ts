import { solve } from './day4_1';
import { setDay, withTestInput } from '../common';

setDay(4);

test('Test answer for Day 4 part 1', async () => {
  expect(await withTestInput(solve)).toBe(18 /*TODO: Fill me in!*/);
});
